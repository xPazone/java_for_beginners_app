import 'package:flutter/material.dart';

class JavaClassMethods extends StatefulWidget {
  @override
  _JavaClassMethodsState createState() => _JavaClassMethodsState();
}

class _JavaClassMethodsState extends State<JavaClassMethods> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          backgroundColor: Colors.lightBlue[900],
          title: Text("Java Classes / Objects",
              style: TextStyle(color: Colors.orange[500]))),
      body: SingleChildScrollView(
        child: Column(
          children: <Widget>[

          ],
        ),
      ),
    );
  }
}
